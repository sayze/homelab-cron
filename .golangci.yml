version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - gosec
    - misspell
    - unconvert
    - unparam

formatters:
  enable:
    - gofumpt
  settings:
    gofumpt:
      module-path: homelab-cron
