services:
  homelab-cron:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${ADDR_HOST_PORT:-8080}:8080"
    environment:
      ADDR: ":8080"
      HOST_ROOT: ${HOST_ROOT:-/host}
    volumes:
      - ${HOST_ROOT_HOST_PATH:-/}:${HOST_ROOT:-/host}:ro
